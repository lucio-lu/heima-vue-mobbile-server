const server_port = 3001;

const hostname = 'localhost'

const address = 'http://' + hostname + ':' + server_port

module.exports = {
    server_port: server_port,
    address: address,
    img_address: '/public/image/thum/'

}